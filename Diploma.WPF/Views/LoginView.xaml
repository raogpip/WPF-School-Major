<Window
    x:Class="Diploma.WPF.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Diploma.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:Diploma.WPF.ViewModels"
    Title="Login"
    Width="350"
    Height="550"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Window.DataContext>
        <viewmodels:LoginViewModel />
    </Window.DataContext>

    <Grid>
        <StackPanel
            Height="300"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical">

            <TextBlock
                FontFamily="Montserrat"
                FontSize="40"
                Text="SCHOOL MAJOR" />

            <TextBlock Margin="0,40,0,0" Text="Username" />
            <TextBox x:Name="txtUsername" Margin="0,7,0,0" />
            <TextBlock Margin="0,10,0,0" Text="Password" />
            <PasswordBox x:Name="txtPassword" Margin="0,7,0,0" />

            <Button
                Margin="0,30,0,0"
                Command="{Binding LoginCommand}"
                Content="Log in" />
        </StackPanel>
    </Grid>
</Window>
